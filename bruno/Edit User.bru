meta {
  name: Edit User
  type: http
  seq: 13
}

put {
  url: {{localhost}}/users/{{id}}
  body: json
  auth: inherit
}

body:json {
  {"name": "John Updated", "email": "john.updated@example.com"}
}

settings {
  encodeUrl: true
}
